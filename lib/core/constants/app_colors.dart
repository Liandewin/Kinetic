import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary — Kinetic Green (from Figma) ─────────────
  static const Color primary = Color(0xFF00E639);
  static const Color primaryDark = Color(0xFF00B82D);

  // ── Light Theme ──────────────────────────────────────
  static const Color backgroundLight = Color(0xFFF9F9F9);
  static const Color cardWhite = Color(0xFFFFFFFF);
  static const Color cardGray = Color(0xFFEEEEEE);
  static const Color cardMapBg = Color(0xFFF3F3F4);

  static const Color textPrimary = Color(0xFF1A1C1C);
  static const Color textSecondary = Color(0xFF777777);
  static const Color textMuted = Color(0xFFAAAAAA);

  static const Color borderLight = Color(0xFFE8E8E8);
  static const Color dividerLight = Color(0xFFF3F3F3);

  // ── Semantic ─────────────────────────────────────────
  static const Color heartRateRed = Color(0xFFBA1A1A);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFFF4757);

  // ── Nav ──────────────────────────────────────────────
  static const Color navBackground = Color(0xFFFFFFFF);
  static const Color navActive = Color(0xFF00E639);
  static const Color navInactive = Color(0xFF777777);

  // ── Shadow ───────────────────────────────────────────
  static const Color shadowColor = Color(0xFF1A1C1C);
}
